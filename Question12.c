#include <stdio.h>
int main(){
    int n;
    printf("Enter the number you wanna check:");
    scanf("%d",&n);
    if (n>0)
        if(n==0)
        printf("the numbet is zero ");
        else 
        printf("yah it is positive!");
    else 
    printf("the number is negative");
    return 0;
    
}